<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hello.chatting.user.mapper.UserMapper">

    <!-- loginId로 User 조회 -->
    <select id="findUserByLoginId" parameterType="string" resultType="hello.chatting.user.domain.User">
        SELECT id, login_id AS loginId, password, email, name, profile_image,role
        FROM user
        WHERE login_id = #{loginId}
    </select>

    <!-- User 저장 -->
    <insert id="save" parameterType="hello.chatting.user.domain.User" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO user (login_id, password, email, profile_image,name, role)
        VALUES (#{loginId}, #{password}, #{email}, #{profileImage},#{name}, #{role})
    </insert>

    <!-- User 전체 조회 -->
    <select id="findAll" resultType="hello.chatting.user.domain.User">
        SELECT id, login_id AS loginId, password, email, name, profile_image,role
        FROM user
    </select>

</mapper>
